# Test-only dependencies. Kept out of requirements.txt so Railway does not
# install them into the running service.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# moto was previously missing everywhere, which made test_s3_service.py fail at
# import - the S3 tests silently were not running.
pytest==9.0.3
pytest-asyncio==1.4.0
moto==5.2.2
